# Master prompt (local copy at repo root)

The canonical master prompt is **gitignored** and lives outside this app folder:

```
../../docs/MASTER_PROMPT.md
```

From the monorepo root:

```bash
./reference/sync-from-downloads.sh   # copy from ~/Downloads
```

Read that file in full before starting any Jarkabi phase.
